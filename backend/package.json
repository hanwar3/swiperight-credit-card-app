{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && npx vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "encore.dev": "^1.56.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/node": "^25.9.0",
    "typescript": "^5.8.3"
  }
}